Boolean Expressions:
d = (~a & ~b & c) | (a & b & ~c);

e = (~b & c) | (b & ~c);

f = (~a & b & ~c) | (a & ~b & c);

g = (~a & b) | (a & ~b);

h = (~a & b & c) | (a & ~b & ~c);

i = (~a & c) | (a & ~c);
